# Bundle Asset Inventory

This directory holds Task 1 exported vector assets and is the target path for bundle-builder product imagery.

The current Vibma MCP session returned raster exports as image content rather than a filesystem path or text payload, so product PNGs are documented in `docs/figma-audit.md` with exact source node IDs for deterministic export in a follow-up pass.
